// Consolidated: re-export DocumentDetail as Markdown to avoid duplicated renderer logic.
// DocumentDetail is the canonical markdown renderer matching the detail-pane aesthetic.
export { DocumentDetail as Markdown } from '@/components/browse/document-detail';
