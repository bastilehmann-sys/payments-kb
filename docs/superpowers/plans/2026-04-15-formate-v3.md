# Formate v3 — Korrekter Content Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Für alle 31 Formate korrekte Management-Summary, SAP-Mapping und Nachrichtenstruktur aus autoritativen Quellen herleiten und in DB pflegen — Pilot mit pain.001 als Gold-Standard, dann Rollout auf die restlichen 30.

**Architecture:** Schema-Erweiterung (`format_entries` um 7 JSONB/text-Spalten für structure, migrations, feature_defs, character_set, reject_code_group, schema_uri_pattern, region, source_refs, content_status). VersionDetailPanel liest primär aus DB mit Fallback auf `content/formats/*.ts`. Pilot pain.001 aus iso20022.org + SAP-OSS-Notes. Rollout via 7 parallele Subagent-Bundles pro Format-Familie mit festen Quellen-URLs im Prompt.

**Tech Stack:** Next.js 16 (custom — siehe AGENTS.md), Drizzle ORM, Neon Postgres, TypeScript. `gray-matter` nicht relevant (kein YAML). Subagents nutzen WebFetch + WebSearch.

**Spec:** `docs/superpowers/specs/2026-04-15-formate-v3-design.md`

---

## File Structure

**Create:**
- `db/migrations/0011_format_entries_content.sql` — DDL für 9 neue Spalten
- `lib/formats/serialize.ts` — Helper für RegExp ↔ JSON (`serializeFeatureDefs`, `deserializeFeatureDefs`)
- Subagent-Templates implizit in Prompts (nicht als Datei)

**Modify:**
- `db/schema.ts` — 9 neue Felder auf `formatEntries`
- `lib/queries/entries.ts` — `getFormatEntries`, `getFormatVersions` um neue Felder erweitern
- `components/formate/version-detail-panel.tsx` — DB-Werte primär, Registry als Fallback
- `lib/formats/registry.ts` — bleibt als Fallback

**Content-SQL-Files (flüchtig, nicht im Repo):**
- `/tmp/formate-v3-pain-001.sql` — Pilot
- `/tmp/formate-v3-<bundle>.sql` × 7 — Rollout

---

## Phase A — Schema-Migration (Tasks 1-3)

### Task 1: SQL-Migration für die 9 neuen Spalten

**Files:**
- Create: `db/migrations/0011_format_entries_content.sql`

- [ ] **Step 1: Datei anlegen**

```sql
-- format_entries v3: strukturierter Content pro Format
ALTER TABLE format_entries
  ADD COLUMN IF NOT EXISTS structure           jsonb,
  ADD COLUMN IF NOT EXISTS migrations          jsonb,
  ADD COLUMN IF NOT EXISTS feature_defs        jsonb,
  ADD COLUMN IF NOT EXISTS character_set       text,
  ADD COLUMN IF NOT EXISTS reject_code_group   text,
  ADD COLUMN IF NOT EXISTS schema_uri_pattern  text,
  ADD COLUMN IF NOT EXISTS region              text,
  ADD COLUMN IF NOT EXISTS source_refs         jsonb,
  ADD COLUMN IF NOT EXISTS content_status      text;

COMMENT ON COLUMN format_entries.content_status IS 'verified | partial | unknown';
```

- [ ] **Step 2: Gegen Neon ausführen**

```bash
cd /Users/sebastianlehmann/payments-kb
set -a && . ./.env.local && set +a
psql "$DATABASE_URL" -f db/migrations/0011_format_entries_content.sql
```

Erwartet: `ALTER TABLE`, keine Errors.

- [ ] **Step 3: Verifizieren**

```bash
psql "$DATABASE_URL" -c "\d format_entries" | grep -E "structure|migrations|feature_defs|character_set|reject_code_group|schema_uri_pattern|region|source_refs|content_status"
```

Erwartet: 9 neue Zeilen.

- [ ] **Step 4: Commit**

```bash
git add db/migrations/0011_format_entries_content.sql
git commit -m "feat(formate): migration — 9 JSONB/text-Spalten für v3 Content"
```

### Task 2: Drizzle-Schema erweitern

**Files:**
- Modify: `db/schema.ts` — `formatEntries`-Block

- [ ] **Step 1: Felder hinzufügen**

Im `formatEntries`-Objekt (zwischen den bestehenden Feldern und `source_row`) einfügen:

```ts
  structure: jsonb('structure'),
  migrations: jsonb('migrations'),
  feature_defs: jsonb('feature_defs'),
  character_set: text('character_set'),
  reject_code_group: text('reject_code_group'),
  schema_uri_pattern: text('schema_uri_pattern'),
  region: text('region'),
  source_refs: jsonb('source_refs'),
  content_status: text('content_status'),
```

Sicherstellen, dass `jsonb` in den Imports oben ist (falls nicht, aus `drizzle-orm/pg-core` hinzufügen).

- [ ] **Step 2: Type-Check**

```bash
cd /Users/sebastianlehmann/payments-kb && pnpm tsc --noEmit
```

Erwartet: Clean.

- [ ] **Step 3: Commit**

```bash
git add db/schema.ts
git commit -m "feat(formate): schema.ts erweitert um v3 JSONB-Felder"
```

### Task 3: Query-Shape erweitern

**Files:**
- Modify: `lib/queries/entries.ts` — `getFormatEntries` select

- [ ] **Step 1: Felder in `getFormatEntries` select ergänzen**

```bash
cd /Users/sebastianlehmann/payments-kb
grep -n "getFormatEntries\|sap_mapping_einsteiger" lib/queries/entries.ts | head
```

In der `select({...})`-Liste von `getFormatEntries` zusätzlich:

```ts
      structure: formatEntries.structure,
      migrations: formatEntries.migrations,
      feature_defs: formatEntries.feature_defs,
      character_set: formatEntries.character_set,
      reject_code_group: formatEntries.reject_code_group,
      schema_uri_pattern: formatEntries.schema_uri_pattern,
      region: formatEntries.region,
      source_refs: formatEntries.source_refs,
      content_status: formatEntries.content_status,
```

Im `FormatEntry`-Type Export die neuen Felder ergänzen (oder `InferSelectModel` nutzen, falls schon).

- [ ] **Step 2: Type-Check**

```bash
pnpm tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add lib/queries/entries.ts
git commit -m "feat(formate): query liest v3 Felder aus format_entries"
```

---

## Phase B — VersionDetailPanel liest aus DB (Tasks 4-6)

### Task 4: FeatureDef-Serialisierungs-Helper

**Files:**
- Create: `lib/formats/serialize.ts`

- [ ] **Step 1: Helper schreiben**

```ts
import type { FeatureDef } from './types';

export type FeatureDefSerialized = {
  pattern: string;
  flags: string;
  name: string;
  what: string;
  tokens: string[];
};

export function serializeFeatureDef(f: FeatureDef): FeatureDefSerialized {
  return {
    pattern: f.match.source,
    flags: f.match.flags,
    name: f.name,
    what: f.what,
    tokens: f.tokens,
  };
}

export function deserializeFeatureDef(s: FeatureDefSerialized): FeatureDef {
  return {
    match: new RegExp(s.pattern, s.flags),
    name: s.name,
    what: s.what,
    tokens: s.tokens,
  };
}

export function serializeFeatureDefs(list: FeatureDef[]): FeatureDefSerialized[] {
  return list.map(serializeFeatureDef);
}

export function deserializeFeatureDefs(raw: unknown): FeatureDef[] {
  if (!Array.isArray(raw)) return [];
  return raw.map((s) => deserializeFeatureDef(s as FeatureDefSerialized));
}
```

- [ ] **Step 2: Type-Check + Commit**

```bash
pnpm tsc --noEmit
git add lib/formats/serialize.ts
git commit -m "feat(formate): FeatureDef JSON-Serialisierung (RegExp ↔ String)"
```

### Task 5: `FormatContent` aus DB-Zeile bauen — Helper

**Files:**
- Modify: `lib/formats/registry.ts`

- [ ] **Step 1: Helper `formatContentFromDb` exportieren**

```ts
// NEW at end of file
import type { FormatContent, StructureNode, Migration } from './types';
import { deserializeFeatureDefs } from './serialize';

export function formatContentFromDb(row: Record<string, unknown>): FormatContent | undefined {
  // Nur dann als content verwenden, wenn mindestens structure vorhanden
  if (!row.structure && !row.migrations && !row.feature_defs) return undefined;
  return {
    formatName: String(row.format_name ?? ''),
    structure:           (row.structure         as StructureNode[] | null) ?? undefined,
    migrations:          (row.migrations        as Migration[] | null)     ?? undefined,
    featureDefs:         deserializeFeatureDefs(row.feature_defs) || undefined,
    characterSet:        (row.character_set     as any) ?? undefined,
    rejectCodeGroup:     (row.reject_code_group as any) ?? undefined,
    schemaUriPattern:    (row.schema_uri_pattern as string | null) ?? undefined,
    region:              (row.region            as string | null) ?? undefined,
  };
}
```

- [ ] **Step 2: Type-Check + Commit**

```bash
pnpm tsc --noEmit
git add lib/formats/registry.ts
git commit -m "feat(formate): formatContentFromDb Helper (DB-Zeile → FormatContent)"
```

### Task 6: VersionDetailPanel bevorzugt DB-Content

**Files:**
- Modify: `app/(fullscreen)/formate/formate-client.tsx` — renderDetail-Closure

- [ ] **Step 1: In `renderDetail`-Callback DB-Content bevorzugen**

Vorher (vereinfacht):
```tsx
renderDetail={(item) => {
  const formatName = String(item.format_name ?? '');
  const content = getFormat(formatName);
  return <VersionDetailPanel ... content={content} />;
}}
```

Nachher:
```tsx
renderDetail={(item) => {
  const formatName = String(item.format_name ?? '');
  const dbContent = formatContentFromDb(item as Record<string, unknown>);
  const registryContent = getFormat(formatName);
  const content = dbContent ?? registryContent;
  return <VersionDetailPanel ... content={content} />;
}}
```

Import hinzufügen: `import { formatContentFromDb } from '@/lib/formats/registry';`

- [ ] **Step 2: Type-Check**

```bash
pnpm tsc --noEmit
```

- [ ] **Step 3: Sanity-Smoke**

```bash
curl -s -o /dev/null -w "status=%{http_code}\n" http://localhost:3000/formate
```

Erwartet: 200 oder 302.

- [ ] **Step 4: Commit**

```bash
git add 'app/(fullscreen)/formate/formate-client.tsx'
git commit -m "feat(formate): VersionDetailPanel liest primär aus DB, Fallback Registry"
```

---

## Phase C — Pilot pain.001 (Tasks 7-9)

### Task 7: Subagent-Prompt — pain.001 aus iso20022.org + SAP-OSS

**Files:**
- Keine (Subagent-Dispatch)

- [ ] **Step 1: Subagent dispatchen (Opus-Modell, Foreground)**

Der Subagent hat folgenden Auftrag:

```
Recherchiere pain.001 (Customer Credit Transfer Initiation) gegen offizielle Quellen und generiere ein einziges SQL-UPDATE-Statement für format_entries WHERE format_name='pain.001'.

Pflicht-Quellen (WebFetch):
1. https://www.iso20022.org/iso-20022-message-definitions — Navigation zu 'pain.001.001.09', extrahiere MessagePartsReport (HTML-Tabelle mit Elementen, Cardinality, Type)
2. https://www.europeanpaymentscouncil.eu/document-library — SCT Scheme Customer-to-Bank IG (neueste Version 2025)
3. WebSearch SAP-OSS-Notes für pain.001: 1844160, 2178617, 2215432, 3268290 — extrahiere Customizing-Pfade und TCODES

Output-Felder:
- beschreibung_einsteiger: neu geschrieben, 3-5 Bullet-Punkte aus Business-Overview der Spec
- sap_mapping_einsteiger: 5-8 Sätze Klartext, welche SAP-Module/TCODES/Tabellen, Verweis auf eine konkrete OSS-Note
- sap_mapping_experte: tief, mit Customizing-Pfaden (SPRO), DMEE-Baum SEPA_CT, OB83, FBZP, BSEG/REGUH/REGUP
- structure (JSONB): vollständiger Baum StructureNode[] — mindestens GrpHdr/PmtInf/CdtTrfTxInf mit je 5-10 Sub-Elementen, Cardinality aus Spec 1:1 übernommen, type = ISO-Type (z.B. Max35Text, PartyIdentification135), desc = Kurz-Beschreibung aus Spec
- migrations (JSONB): Migration von 001.001.03 → 001.001.09 mit 10-15 Changes aus EPC SCT Rulebook Version History
- feature_defs (JSONB): FeatureDefSerialized[] — 6-8 patterns für 001.001.09 (PostalAddress24, BICFI, LEI, UETR, ReqdExctnDt, UltimateOriginator, strukturierter Remittance, UID-Formate)
- character_set: 'sepa-latin'
- reject_code_group: 'iso20022'
- schema_uri_pattern: 'urn:iso:std:iso:20022:tech:xsd:pain.001.001.<v>'
- region: 'Global / SEPA'
- source_refs: [{url: '...', retrieved_at: '2026-04-15'}, ...] — alle tatsächlich abgerufenen URLs
- content_status: 'verified' oder 'partial'

Qualitäts-Regeln:
- Keine Elementnamen oder Cardinalities erfinden — wenn Quelle nicht zugänglich, Feld weglassen und in content_status 'partial' markieren, in source_refs einen Comment-URL mit Begründung aufnehmen
- Beschreibungen wörtlich oder paraphrasiert aus Spec, nicht aus LLM-Wissen

Output: SQL-File /tmp/formate-v3-pain-001.sql mit einem einzigen Statement:

UPDATE format_entries SET
  beschreibung_einsteiger = $$...$$,
  sap_mapping_einsteiger  = $$...$$,
  sap_mapping_experte     = $$...$$,
  structure               = $$[{...}]$$::jsonb,
  migrations              = $$[{...}]$$::jsonb,
  feature_defs            = $$[{...}]$$::jsonb,
  character_set           = 'sepa-latin',
  reject_code_group       = 'iso20022',
  schema_uri_pattern      = 'urn:iso:std:iso:20022:tech:xsd:pain.001.001.<v>',
  region                  = 'Global / SEPA',
  source_refs             = $$[{...}]$$::jsonb,
  content_status          = 'verified'
WHERE format_name = 'pain.001';
```

Dispatch:
```
Agent — general-purpose, opus, run_in_background=false, prompt wie oben
```

- [ ] **Step 2: SQL-File prüfen**

```bash
ls -la /tmp/formate-v3-pain-001.sql
head -40 /tmp/formate-v3-pain-001.sql
```

### Task 8: SQL anwenden + Review-URL öffnen

- [ ] **Step 1: SQL gegen DB**

```bash
cd /Users/sebastianlehmann/payments-kb
set -a && . ./.env.local && set +a
psql "$DATABASE_URL" -f /tmp/formate-v3-pain-001.sql
```

Erwartet: `UPDATE 1`.

- [ ] **Step 2: Status-Check**

```bash
psql "$DATABASE_URL" -c "SELECT format_name, content_status, jsonb_array_length(structure) AS top_nodes, jsonb_array_length(migrations) AS migrations, jsonb_array_length(feature_defs) AS features, jsonb_array_length(source_refs) AS sources FROM format_entries WHERE format_name='pain.001';"
```

Erwartet: `content_status='verified'`, `top_nodes >= 2`, `sources >= 2`.

- [ ] **Step 3: Review-URL**

User öffnet `http://localhost:3000/formate?id=<pain.001.001.09-id>` und prüft:
- Management Summary (bullet-list, 3-5 Punkte)
- SAP-Mapping permanent (blauer Kasten, sollte jetzt den neuen Text zeigen)
- Tab „Aufbau & Felder" → Strukturbaum mit Elementen aus DB
- Tab „Was ist neu v.09" → Migration-Diff aus DB

### Task 9: Pilot-Review-Schleife

- [ ] **Step 1: User gibt Freigabe oder Korrekturen**

Wenn User Freigabe: weiter Phase E.
Wenn Korrekturen: Subagent erneut dispatchen mit User-Feedback als zusätzlichem Kontext, bis Approval.

- [ ] **Step 2: Pilot-Ergebnis als Template sichern**

Das approved pain.001-SQL ist ab jetzt die **strukturelle Vorlage** für alle 7 Rollout-Bundles — Subagents bekommen Auszüge als Referenz.

```bash
cp /tmp/formate-v3-pain-001.sql ~/payments-kb/docs/superpowers/specs/2026-04-15-formate-v3-pain-001-template.sql
git add docs/superpowers/specs/2026-04-15-formate-v3-pain-001-template.sql
git commit -m "docs(formate): pain.001 v3 SQL als Template für Rollout archiviert"
```

---

## Phase E — Rollout auf 30 Formate (Tasks 10-16)

Pro Bundle: ein Subagent, Output ein SQL-File, Orchestrator fährt `psql -f`, User reviewt Stichprobe (1 Format pro Bundle).

### Task 10: Bundle pain-rest (pain.002, .003, .007, .008, .013)

- [ ] **Step 1: Subagent dispatchen**

```
Agent (opus): Generiere content/format-Update für 5 pain-Formate: pain.002, pain.003, pain.007, pain.008, pain.013.

Quellen (WebFetch):
- https://www.iso20022.org/iso-20022-message-definitions (pro Format MessagePartsReport)
- https://www.europeanpaymentscouncil.eu/document-library (SDD Scheme für pain.008; SRTP für pain.013)
- SAP-OSS-Notes per Format (WebSearch 'SAP pain.002 OSS', etc.)

Für jedes Format genauso wie Pilot pain.001 (siehe docs/superpowers/specs/2026-04-15-formate-v3-pain-001-template.sql als Vorlage):
- beschreibung_einsteiger (3-5 Bullets aus Spec)
- sap_mapping_einsteiger + sap_mapping_experte
- structure: StructureNode[] aus ISO-Spec — min. 10 Top-Level-Felder
- migrations: 1 Migration pro Format (oder leer wenn keine Breaking-Changes)
- feature_defs: 3-6 patterns aus jüngsten Versions-Notes
- character_set='sepa-latin', reject_code_group='iso20022' (außer pain.007: auch iso20022)
- schema_uri_pattern per Format
- source_refs mit URLs + retrieved_at='2026-04-15'
- content_status='verified' (oder 'partial' bei Lücken)

Output: EIN SQL-File /tmp/formate-v3-pain-rest.sql mit 5 UPDATE-Statements.
```

- [ ] **Step 2: SQL anwenden**

```bash
psql "$DATABASE_URL" -f /tmp/formate-v3-pain-rest.sql
```

Erwartet: `UPDATE 1` fünfmal.

- [ ] **Step 3: Stichprobe**

User öffnet pain.008 in `/formate`, prüft plausible Cardinalities + SAP-Mapping.

- [ ] **Step 4: Commit (Template-Kopie)**

```bash
cp /tmp/formate-v3-pain-rest.sql docs/superpowers/specs/2026-04-15-formate-v3-pain-rest.sql
git add docs/superpowers/specs/2026-04-15-formate-v3-pain-rest.sql
git commit -m "docs(formate): pain-rest v3 SQL archiviert"
```

### Task 11: Bundle camt (camt.052, .053, .054)

- [ ] **Step 1: Subagent**

Analog Task 10, Quelle iso20022.org (pro camt-Format MessagePartsReport). character_set='sepa-latin', reject_code_group=null (camt = Reports). Output /tmp/formate-v3-camt.sql mit 3 UPDATE-Statements.

- [ ] **Step 2-4: Apply, Stichprobe (z.B. camt.053), Commit.**

### Task 12: Bundle pacs-gpi (pacs.008, CBPR+, MX gpi)

- [ ] **Step 1: Subagent**

Quelle: iso20022.org + SWIFT CBPR+ Usage Guidelines (public). character_set='utf-8'. /tmp/formate-v3-pacs-gpi.sql mit 3 UPDATE-Statements.

- [ ] **Step 2-4**: Apply (3×UPDATE), Stichprobe CBPR+, Commit.

### Task 13: Bundle swift-mt (MT101, MT103, MT202, MT940, MT942)

- [ ] **Step 1: Subagent**

Quelle: WebSearch nach MT-Tag-Strukturen (Paiementor, SWIFT-Community-Docs). character_set='swift-x', reject_code_group='swift-mt'. structure als flache Tag-Liste (`name=':20:'`, type='Max16Text', card='1'/'0..1', desc=Beschreibung). 5 UPDATE-Statements.

- [ ] **Step 2-4**: Apply, Stichprobe MT103, Commit.

### Task 14: Bundle eu-legacy (Bacs, CLIEOP, Cuaderno 34, DTAUS, DTAZV, LCR-MAG, Faster Payments)

- [ ] **Step 1: Subagent**

Gemischte Quellen: Bundesbank (DFÜ für DTAUS/DTAZV), pay.uk (Bacs/FPS), aebanca.es (Cuaderno), cfonb.org (LCR-MAG). CLIEOP abgekündigt — `content_status='partial'` akzeptieren. /tmp/formate-v3-eu-legacy.sql mit 7 UPDATE-Statements.

- [ ] **Step 2-4**: Apply, Stichprobe DTAZV, Commit.

### Task 15: Bundle italy-local (CBI, FatturaPA, RIBA, SEDA)

- [ ] **Step 1: Subagent**

Quellen: cbi-org.eu, fatturapa.gov.it, abi.it. FatturaPA v1.2 Schema als authoritative Struktur. /tmp/formate-v3-italy-local.sql mit 4 UPDATE-Statements.

- [ ] **Step 2-4**: Apply, Stichprobe FatturaPA, Commit.

### Task 16: Bundle us-jp-local (Fedwire FAIM, NACHA, Zengin Text)

- [ ] **Step 1: Subagent**

Quellen: frbservices.org (Fedwire FAIM → ISO20022 pacs.008 ab März 2025), achdevguide.nacha.org, zenginkyo.or.jp. reject_code_group='nacha' für NACHA. /tmp/formate-v3-us-jp-local.sql mit 3 UPDATE-Statements.

- [ ] **Step 2-4**: Apply, Stichprobe NACHA, Commit.

---

## Phase F — Final Verification + Push (Task 17-18)

### Task 17: Komplett-Verifikation

- [ ] **Step 1: DB-Status aller 31 Formate**

```bash
cd /Users/sebastianlehmann/payments-kb
set -a && . ./.env.local && set +a
psql "$DATABASE_URL" -c "
SELECT
  format_name,
  content_status,
  CASE WHEN structure IS NULL THEN 0 ELSE jsonb_array_length(structure) END AS struct_nodes,
  CASE WHEN migrations IS NULL THEN 0 ELSE jsonb_array_length(migrations) END AS migs,
  CASE WHEN feature_defs IS NULL THEN 0 ELSE jsonb_array_length(feature_defs) END AS features,
  CASE WHEN source_refs IS NULL THEN 0 ELSE jsonb_array_length(source_refs) END AS sources
FROM format_entries
ORDER BY format_name;
"
```

Erwartet: alle 31 Formate mit `content_status IN ('verified','partial')`, `struct_nodes >= 10`, `sources >= 2`.

- [ ] **Step 2: Browser-Rundcheck** — User öffnet pro Bundle je 1 Format in `/formate`, prüft dass alles rendert.

- [ ] **Step 3: Type-Check + Build**

```bash
pnpm tsc --noEmit
pnpm build 2>&1 | tail -20
```

Erwartet: 0 Errors, Build Succeeds.

### Task 18: Push + Vercel Deploy

- [ ] **Step 1**

```bash
cd /Users/sebastianlehmann/payments-kb
git push origin main
```

Vercel deployt automatisch. Nach ~2 Min ist `https://payments-kb.vercel.app/formate` live mit v3-Content.

---

## Coverage-Checkliste gegen Spec

- [x] Phase A: Schema-Migration → Tasks 1-3
- [x] Phase B: VersionDetailPanel liest aus DB → Tasks 4-6
- [x] Phase C: Pilot pain.001 → Tasks 7-9
- [x] Phase D: Review-Schleife → Task 9
- [x] Phase E: Rollout 7 Bundles → Tasks 10-16
- [x] Source-Map pro Familie in Subagent-Prompts → jeweils Task X Step 1
- [x] Qualitäts-Regeln (keine Halluzination, source_refs, content_status) → Task 7 Prompt, propagiert in alle Rollout-Tasks
- [x] Schema-Zusatz (source_refs, content_status) → Task 1 (in Migration enthalten)
- [x] FeatureDef JSON-Serialisierung → Task 4
- [x] Fallback auf Registry bei fehlendem DB-Content → Task 6 (`content = dbContent ?? registryContent`)
- [x] Out-of-Scope (kein Cron, kein Inline-Editor für Struktur, keine Versionierung) — explizit in Spec, keine Tasks
- [x] Erfolgs-Kriterium (Summary 3-5 Bullets, SAP 3+ TCODES, Struktur 10+ Top-Level-Felder, 2+ source_refs, content_status gesetzt) → Task 17 Step 1 prüft das DB-seitig
